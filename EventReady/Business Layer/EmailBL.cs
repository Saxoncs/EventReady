using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using static EventReady.Data_Access_Layer.EmailDataAccess;

namespace EventReady.Business_Layer
{
    public class EmailBL
    {
        //Needs to get the user id from session data, should otherwise return a list of guests associated with the logged in user to be worked with by the user layer
        //List<Guest> guestList = GetGuests(*insert userId from session here*);

    }
}